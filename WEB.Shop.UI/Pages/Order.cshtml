@page "{reference}"
@model WEB.Shop.UI.Pages.OrderModel
@{
}

<div>
    <h1>
        @Model.Order.OrderReference
    </h1>
    
    @foreach (var product in Model.Order.Products)
    {
        <div>
            @product.Name - @product.Description - @product.Quantity - @product.Value
        </div>
    }

</div>